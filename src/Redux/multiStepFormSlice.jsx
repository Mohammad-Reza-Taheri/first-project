import { createSlice } from "@reduxjs/toolkit";
const initialState={
value:0
}



const multiStepFormSlice=createSlice({
    name:"multiStepForm",
    initialState,
    reducer:{

    }
})
export const {} = multiStepFormSlice.actions
export default multiStepFormSlice.reducer